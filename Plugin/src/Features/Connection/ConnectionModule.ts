import { ConnectionController } from "./Controllers/ConnectionController";

// This is just for reference - the module pattern is handled differently in this project
export const ConnectionFeature = {
	Controller: ConnectionController,
};
